fastapi
uvicorn[standard]
python-dotenv
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.18.5
cryptography
httpx
# Cliente del ciclo de Descarga Masiva de CFDI del SAT (zg55DWY):
# Solicita -> Verifica -> Descarga. Pin exacto (la API de retorno de
# cada metodo se confirmo contra esta version). Arrastra requests, lxml,
# pycryptodome, pyOpenSSL y sube cryptography a >=46 (APIs usadas por el
# codigo de e.firma son estables en esa version).
cfdiclient==1.6.3
# Subida de los .zip de la descarga masiva a MinIO (bucket cfdi-descargas-sat),
# mismo patron que facturacion/storage_client.py.
minio
# Upload de logo de white-label (zg2mOhE, POST /admin/config/logo) - FastAPI
# necesita esto para parsear multipart/form-data (UploadFile). Mismo pin que
# whatsapp_bot/requirements.txt.
python-multipart==0.0.12
# Lectura de la CAPA DE TEXTO de PDFs de declaraciones anuales (reporte
# 189) - solo texto, nunca OCR/imagenes. Elegido sobre PyPDF2 (que
# whatsapp_bot ya usa, pero esta EN DESUSO - el propio proyecto redirige
# a pypdf, su sucesor mantenido) y sobre pdfplumber/PyMuPDF (dependencias
# de sistema mas pesadas, no necesarias solo para extraer texto). Pura
# Python, sin dependencias de sistema (poppler/mupdf).
pypdf==6.19.0
