# Dependencias 
node_modules/ 
__pycache__/ 
*.pyc 
*.pyo 
.venv/ 
venv/ 
*.egg-info/ 
 
# Variables de entorno — NUNCA subir a Git
.env
.env.*
!.env.example
# Plantillas de Staging/Produccion (13 ago 2026) - mismo criterio que
# .env.example: nunca secretos reales, solo placeholders documentados.
!.env.staging.example
!.env.production.example
 
# Python test / cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.db
test_output.txt
 
# Build frontend 
frontend/dist/ 
frontend/build/ 
 
# Docker
*.log
compose_start_log.txt

# Capturas locales de ngrok (exponen IP/tunnels de desarrollo)
ngrok_requests.json
 
# IDE
.vscode/settings.json
.idea/
*.swp

# CSD/FIEL de prueba — nunca deben subirse a git
certs_test/
certs_org/
*.cer
*.key
*.sdg
experiments/sat-descarga-masiva/.env

# Evidencia/scratch de sesiones de trabajo (Claude Code) - nunca
# debe vivir en la raiz del repo, solo en Downloads/CFDI local.
# Anclado a la raiz (/) para no afectar .txt/.json legitimos en
# subcarpetas (fixtures de test, catalogos, migraciones, etc.).
/*.txt
/*.json
