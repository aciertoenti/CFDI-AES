[pytest]
asyncio_mode = auto
# scripts/ no esta en sys.path por defecto (solo la raiz /app lo esta,
# de ahi que "from database import ..." ya funcione en los tests) -
# necesario para que tests/test_rotar_llave.py pueda hacer
# "from rotar_llave import rotar" (reporte 186).
pythonpath = scripts
