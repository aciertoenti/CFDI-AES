fastapi
uvicorn[standard]
httpx
satcfdi
python-dotenv
zeep
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.18.5
minio
# weasyprint/jinja2/qrcode (04 sep 2026, zg5b-ZE PDF de ticket con QR):
# ya llegaban instaladas como dependencia transitiva de satcfdi (que las usa
# para su propio render de PDF de CFDI), pero main.py ahora las importa
# directo (import weasyprint/jinja2/qrcode) - declaradas aqui explicitas
# para no depender de que satcfdi las siga requiriendo en el futuro.
weasyprint
jinja2
qrcode
# redis (05 sep 2026, zg5b-ZE pieza 5): rate limiting por qr_token del
# endpoint publico de autofacturacion individual. Mismo mecanismo (TTL
# nativo, sin tabla ni migracion) y mismo patron de modulo singleton que
# auth_usuarios/redis_client.py - ver redis_client.py en este servicio.
redis
# APScheduler (18 sep 2026, g7imYM pieza 3): cierre automatico de
# consolidacion de Publico en General - primer scheduler del proyecto
# (confirmado antes de agregarlo que no existia ninguno, ver comentario en
# zg6k9Ok/administracion). AsyncIOScheduler especificamente (no
# BackgroundScheduler) - corre en el mismo event loop asyncio que el resto
# del servicio, ver comentario junto al import en main.py.
apscheduler
